services:
  frontend-dev:
    image: node:20-alpine
    container_name: tourism-dashboard-dev
    working_dir: /app
    volumes:
      - /Users/enescanbilgic/Desktop/Projeler/Okul/tourism-dashboard:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "5173:5173"
